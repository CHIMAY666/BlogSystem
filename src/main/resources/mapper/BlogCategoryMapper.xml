<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.xmu.blogsystem.dao.BlogCategoryMapper">
    <select id="getAllCategories">
        select *
        from blog_category
        where is_deleted = 0
    </select>
    <update id="deleteBatch">
        update blog_category
        set is_deleted=1 where category_id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
    <insert id="insert" parameterType="edu.xmu.blogsystem.entity.BlogCategory">
        insert into blog_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="categoryId != null">
                category_id,
            </if>
            <if test="categoryName != null">
                category_name,
            </if>
            <if test="categoryRank != null">
                category_rank,
            </if>
            <if test="isDeleted != null">
                is_deleted,
            </if>
            <if test="createTime != null">
                create_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="categoryId != null">
                #{categoryId},
            </if>
            <if test="categoryName != null">
                #{categoryName},
            </if>
            <if test="categoryRank != null">
                #{categoryRank},
            </if>
            <if test="isDeleted != null">
                #{isDeleted},
            </if>
            <if test="createTime != null">
                #{createTime},
            </if>
        </trim>
    </insert>
    <select id="selectById">
        select *
        from blog_category
        where category_id = #{categoryId} AND is_deleted = 0
    </select>
    <select id="selectByCategoryName">
        select *
        from blog_category
        where category_name = #{categoryName} AND is_deleted = 0
    </select>
    <update id="updateById" parameterType="edu.xmu.blogsystem.entity.BlogCategory">
        update blog_category
        <set>
            <if test="categoryName != null">
                category_name = #{categoryName},
            </if>
            <if test="categoryRank != null">
                category_rank = #{categoryRank},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where category_id = #{categoryId}
    </update>
    <select id="getTotalCategories">
        select count(*) from blog_category
        where is_deleted=0
    </select>
</mapper>